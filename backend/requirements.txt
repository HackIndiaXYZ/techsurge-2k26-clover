pydantic>=2.0,<3.0

# Needed only for model/train_scorecard.py and model/validate.py (offline
# training and evaluation). model/scorecard.py, model/reason_codes.py and
# model/artifact.py do inference with plain stdlib math over the saved
# artifact's JSON, so a deployed API does not need numpy/scikit-learn at all.
numpy>=1.26
scikit-learn>=1.4

# The live API server (backend/api/). httpx is only needed to run
# backend/tests/test_api.py's FastAPI TestClient, not to serve the app.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
